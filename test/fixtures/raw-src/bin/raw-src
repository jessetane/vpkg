#!/usr/bin/env bash
#
#	raw-src (no vpkgfile)
#

# own dir
cd "$(dirname "$BASH_SOURCE")"

# if --version was passed, just print the version
if [ "$1" = "--version" ]
then
  echo "$(basename "$(dirname "$(pwd)")")"
  exit 0
fi

# if we made it this far, say hello
echo "hello from raw-src: a package with no vpkgfile"